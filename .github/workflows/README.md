# GitHub is a public install mirror

**Source of truth:** LastGit `lastdb:///lastsecrets` (code node).

GitHub Actions are inert. Do not re-enable push/PR CI. Forgejo may remain a
rollback remote; merge policy is LastGit only.
